import { BreakpointDirective } from './breakpoint.directive';

describe('BreakpointDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakpointDirective();
    expect(directive).toBeTruthy();
  });
});
